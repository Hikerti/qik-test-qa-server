[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "libs-infrastructure"
version = "0.1.0"
description = "Infrastructure library for AI project"


