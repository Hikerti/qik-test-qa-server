[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "libs-integrations"
version = "0.1.0"
description = "Integrations library for AI project"


