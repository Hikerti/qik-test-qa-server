[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-api"
version = "0.1.0"
description = "FastAPI orchestrator for AI test generation"


