[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-generator"
version = "0.1.0"
description = "AI Generator service - core functionality for prompt-based generation"

