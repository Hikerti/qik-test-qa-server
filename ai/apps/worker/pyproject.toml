[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai-worker"
version = "0.1.0"
description = "Background worker for AI test generation"


