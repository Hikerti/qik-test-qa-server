[build-system]
requires = ["setuptools>=61.0", "wheel"]
build-backend = "setuptools.build_meta"

[project]
name = "ai"
version = "0.1.0"
description = "AI subproject for automated test generation"
requires-python = ">=3.10"

[tool.setuptools]
packages = []


