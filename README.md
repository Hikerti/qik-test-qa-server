## 🏛️ Структура Проекта и Инфраструктура

Архитектура проекта четко разделена на два основных блока: **Конечные Приложения (`apps`)** и **Общие Библиотеки (`libs`)**, обеспечивающие модульность и повторное использование кода.

---

### 📦 Блок `apps/` (Конечные Сервисы)

Этот каталог содержит основные доменные сущности и точки входа (ручки), реализующие конкретную бизнес-логику.

| Содержимое | Описание | Ключевые Компоненты |
| :--- | :--- | :--- |
| **Доменные Сущности и Ручки** | Реализация конечных сервисов, которые взаимодействуют с внешней средой и хранилищами. | **Контроллеры** (API Endpoints), **Сервисы** (Бизнес-логика), **Репозитории** (Слой доступа к данным). |

### 🧩 Блок `libs/` (Общие Библиотеки)

Этот блок отвечает за инкапсуляцию вспомогательного, инфраструктурного и интеграционного кода, доступного для многократного использования в каталоге `apps/`.

#### 📚 Основные Модули

| Каталог | Назначение | Описание |
| :--- | :--- | :--- |
| **`libs/shared`** | **Утилиты и Общие Типы** | Слой вспомогательных функций, общих **типов**, **декораторов** и других переиспользуемых утилит, не связанных напрямую с бизнес-логикой или инфраструктурой. |
| **`libs/agents`** | **Описание Агентов** | Содержит логику и конфигурации для **агентов** (например, AI- или автоматизированных процессов). |

#### 🌐 Интеграции и Системы

| Каталог | Назначение | Описание |
| :--- | :--- | :--- |
| **`libs/integrations`** | **Внешние Интеграции** | Описания и интерфейсы для взаимодействия с **внешними сервисами** (например, платежные системы, сторонние API). **Не содержит** бизнес-логики проекта. |
| **`libs/systems`** | **Сервисы Реализации** | Реализация логики, необходимой для **подключения и работы** с внешними интеграциями, описанными в `integrations`. |

#### 💾 Инфраструктура и Подключения

| Каталог | Назначение | Описание |
| :--- | :--- | :--- |
| **`libs/infractract`** | **Подключения к Хранилищам** | Модули для подключения и настройки наших собственных **сервисов хранения данных** (например, инициализация ORM для баз данных, настройка облачных хранилищ S3/GCS). |

---

### ⚙️ Дополнительная Конфигурация

| Каталог | Назначение | Описание |
| :--- | :--- | :--- |
| **`envs/`** | **Переменные Окружения** | Файлы и конфигурация, связанные с различными средами (Development, Staging, Production). |
| **`deploy/`** | **Развертывание** | Файлы конфигурации для **контейнеризации** и развертывания проекта (например, `Dockerfile`, `docker-compose.yml`). |

Запуск контейнера с nats - docker compose -f deploy/compose.proxy.yml up 